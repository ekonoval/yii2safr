class_name: ApiTester
modules:
    enabled:
      - ApiHelper
      - PhpBrowser
      - REST
      - Db
    config:
        PhpBrowser:
            url: 'http://yii2book.dev'
        REST:
            url: 'http://yii2book.dev'
        Db:
            dsn: 'mysql:host=localhost;dbname=crmapp_test'
            user: 'root'
            password: ''
            dump: tests/_data/dump.sql

